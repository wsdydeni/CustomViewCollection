<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

        <wsdydeni.widget.seekbar.SeekBar
            android:id="@+id/verify_seekbar"
            app:text="向右滑呀"
            app:textColor="@color/colorAccent"
            app:startBitmap="@drawable/ic_launcher_background"
            app:endBitmap="@drawable/ic_test"
            app:backgroundColor="@android:color/holo_green_dark"
            app:dragColor="@android:color/holo_blue_light"
            android:layout_centerInParent="true"
            android:layout_width="match_parent"
            android:layout_marginStart="40dp"
            android:layout_marginEnd="40dp"
            android:layout_height="60dp"/>

</RelativeLayout>
